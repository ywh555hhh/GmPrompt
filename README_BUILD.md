# GM Prompt Builder - 本地构建版

基于 Python 脚本 + VS Code 的灵活 Prompt 管理方案。

## 目录结构

```
GmPrompt/
├── build.py                 # 构建脚本 ⭐
├── output/                  # 输出目录（自动创建）
│   └── 2025-01-22-prompt.md
├── prompts/
│   ├── config.yaml           # 模块配置（可选，未来扩展）
│   └── modules/             # 规则模块
│       ├── 00_system/
│       ├── 01_style/
│       ├── 02_content/
│       ├── 03_format/
│       ├── 04_logic/
│       └── 05_mechanics/
├── stories/                 # 背景故事（独立管理）
│   └── 故事1.yaml
├── .vscode/                # VS Code 配置 ⭐
│   ├── settings.json
│   ├── extensions.json
│   ├── tasks.json
│   ├── keybindings.json
│   └── README.md            # 扩展推荐
└── README.md
```

## 使用方法

### 1. 构建并导出

```bash
# 运行构建
python3 build.py

# 查看输出
ls output/

# 复制到剪贴板
cat output/2025-01-22-prompt.md | pb     # macOS
cat output/2025-01-22-prompt.md | xclip # Linux
```

### 2. 自定义模块选择

编辑 `build.py` 中的 `get_selected_modules()` 函数来选择要包含的模块。

### 3. 管理背景故事

`stories/` 目录独立管理，不在 Prompt 构建流程中。

---

## VS Code 推荐

查看 [`.vscode/README.md`](.vscode/README.md) 获取：
- 推荐扩展列表
- 快捷键配置
- 最佳实践

### 常用快捷键

| 操作 | 快捷键 |
|------|--------|
| 格式化文档 | `Shift + Alt + F` |
| 运行任务（如果配置） | `Cmd + B` (macOS) / `Ctrl + B` (Windows/Linux) |
| 快速打开文件 | `Cmd + P` |
| 切换侧边栏 | `Cmd + B` |

---

## 添加新模块

1. 在对应分类目录下创建 `.md` 文件
2. 运行 `python3 build.py`
3. 输出文件会自动包含新模块

### 示例：添加新模块

```bash
# 在 prompts/modules/02_content/ 下创建新模块
cat > prompts/modules/02_content/新机制.md << 'EOF'
# 新机制名称

## 描述

这是机制的详细说明...

## 使用示例

...

EOF

# 重新构建
python3 build.py
```

---

## 优势

| 特性 | 说明 |
|------|------|
| 🚀 零依赖 | 不需要安装任何工具或依赖 |
| 📦 原生 Git | git 直接追踪所有改动 |
| 🎯 精准控制 | 只启用需要的模块 |
| 🔄 快速迭代 | 改完立即重建，无需刷新 |
| 🤖 AI 辅助 | 用 Cursor/Claude Code 帮你写模块 |
| 📂 灵活存储 | 故事、Prompt、配置分离管理 |

---

## 输出示例

运行 `python3 build.py` 后会看到：

```
==================================================
✅ Prompt 构建完成！
📄 输出文件: output/2025-01-22-prompt.md
📊 字符数: 12,345
🤖 预估 Token: 4,938
==================================================
```
